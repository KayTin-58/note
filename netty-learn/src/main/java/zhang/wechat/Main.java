package zhang.wechat;


/**
 * description
 *
 * @author zb 2019/07/03 20:11
 */
public class Main {

    /*
     * @Test public void encode() { Serializer serializer = new JSONSerializer(); LoginRequestPacket
     * loginRequestPacket = new LoginRequestPacket();
     * 
     * loginRequestPacket.setVersion(((byte) 1)); loginRequestPacket.setUserId("123");
     * loginRequestPacket.setUserName("zhangsan"); loginRequestPacket.setUserPassword("password");
     * 
     * 
     * PacketCodec packetCodeC = PacketCodec.INSTANCE ByteBuf byteBuf =
     * packetCodeC.encode(loginRequestPacket); Packet decodedPacket = packetCodeC.decode(byteBuf);
     * 
     * Assert.assertArrayEquals(serializer.serialize(loginRequestPacket),
     * serializer.serialize(decodedPacket));
     * 
     * }
     */
}
